import React from 'react'
import { Container } from 'react-bootstrap'
import style from './works.module.css'
export default function Works(props) {
  return (
    <div id={props.id} className={style.work__container}>
      <Container>

      </Container>
    </div>
  )
}
